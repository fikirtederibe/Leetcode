class Solution: 
    def select(self, arr, i):
        arr[]
    def selectionSort(self, arr,n):
        for i in range(n):
            for j in range(i+1,n-1):
                if(arr[j]>arr[i]):
                    a[j],a[i]=a[i],a[j]
